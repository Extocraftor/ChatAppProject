name: objective_c
description: Vendored objective_c runtime package without native hook execution for Windows builds.
version: 9.3.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  collection: ^1.19.1
  ffi: ^2.1.0
  pub_semver: ^2.2.0
